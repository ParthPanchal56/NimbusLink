var story = document.getElementsByClassName('story');
var storyContainer = document.getElementsByClassName('story-container');
var hs = new HorizontalScroll.default({
    blocks: story,
    container: storyContainer,
});